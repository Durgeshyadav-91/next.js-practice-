export default function LoginLayout({ children }) {
    return (
      <html lang="en">
          <h1>Hello Login Common page</h1>
        <body>{children}
      
        </body>
      </html>
    );
  }